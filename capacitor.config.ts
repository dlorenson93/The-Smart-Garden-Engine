import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.terraplantari.app',
  appName: 'TerraPlantari',
  webDir: 'frontend/dist'
};

export default config;
